# Reference documents

Locked SHIELD v2 reference material (spec, mockup, questionnaires, playbook,
per-tier interview/profile artifacts). These are inputs to the build; renamed
and relocated here per `DECISIONS.md` D-013. Do not edit them to match the code —
the code is corrected to match reality, and divergences are logged in
`DECISIONS.md`.

## Not in this repo

- **The v2 Work Order** (the remediation tasking that drives Sprints 0–3 — Parts
  B/D/E/F/G/H, issue IDs like `H-3`, `E-6`) lives **outside this repository** and
  is **not** checked in here. It was not provided as a file to vendor, so it is
  intentionally not reproduced or fabricated. Cross-references to Work Order
  parts in `DECISIONS.md`, `CHANGELOG.md`, and code comments point at that
  external document. If it is later supplied, add it here as
  `SHIELDv2_Work_Order.txt` (normalized filename, per D-013) and update this note.
